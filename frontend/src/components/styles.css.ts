import { style } from '@vanilla-extract/css';

export const map = style({
    margin: '1%',
    height: '96%',
    width: '50%',
});

export const chart = style({
    height: '40vh',
    width: '50vw',
});
